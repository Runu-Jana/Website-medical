{
  "name": "dcare-medical-mern",
  "version": "1.0.0",
  "private": true,
  "description": "DCare — Medical & Pharmacy eCommerce platform (MERN + Tailwind) with storefront and admin panel",
  "scripts": {
    "install:all": "npm install --prefix backend && npm install --prefix frontend && npm install --prefix admin",
    "seed": "npm run seed --prefix backend",
    "dev:backend": "npm run dev --prefix backend",
    "dev:frontend": "npm run dev --prefix frontend",
    "dev:admin": "npm run dev --prefix admin",
    "build:frontend": "npm run build --prefix frontend",
    "build:admin": "npm run build --prefix admin"
  },
  "author": "Freelance Web Developer",
  "license": "MIT"
}
